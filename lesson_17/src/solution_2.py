def binary_find_element(data: list, num):
    #a = data[0]
    #b = data[-1]
    c = len(data) / 2
    for i in range(c):
        if num == data[c]:
            print(True)
        if num < data[c]:

def element(data):
    c = len(data) / 2
    if num == data[c]:
        print(True)
    if num < data[c]:
        c = len(data) / 2 /2
    else:
        